<main class="main">
  <div class="inner">
    <% if user_signed_in? %>
      <div class="greeting">
      <a>こんにちは、<span> <%= link_to current_user.name, "/users/#{current_user.id}", class: :greeting__link%></span>さん</a>
      </div>
    <% end %>  
    <div class="card__wrapper">
       <%= render partial: "prototype", collection: @prototypes %>
    </div>
  </div>
</main>
