<main class="main">
  <div class="inner">
    <% if user_signed_in? %>
      <div class="greeting">
      <a>こんにちは、<span> <%= link_to current_user.name, root_path, class: :greeting__link%></span>さん</a>
      </div>
    <% end %>  
    <%# // ログインしているときは上記を表示する %>
    <div class="card__wrapper">
      <%# 投稿機能実装後、部分テンプレートでプロトタイプ投稿一覧を表示する %>
    </div>
  </div>
</main>
